""" A params file to aid with the tree visualization. """

params = {
    'plot_tree': False,
    'save_tree_image': False,
    'axis_tree_equal': True,    # If true keeps both axis same size, otherwise contracts one axis
}
